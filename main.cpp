#include "Game.hpp"

int main()
{
    Game().LaunchPlayerGame();

    // use arg to see if ai game is asked.
    // if it is, check number of match asked. if not specified make 0 matches.
    // at the end of each match, fuse the AI's files.
}
